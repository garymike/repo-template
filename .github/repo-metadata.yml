# Declare the intended visibility and purpose of this repo.
# The github-security-audit skill checks this against actual GitHub settings
# and flags any mismatch as a finding.
#
# visibility: public | private
# purpose: free-text description of what this repo is for

visibility: public
purpose: >
  GitHub repository template pre-wired with security defaults — gitleaks,
  Dependabot, weekly security audit via security-workflows, SECURITY.md,
  PR template, .editorconfig, and .gitignore. Use when creating any new repo.
